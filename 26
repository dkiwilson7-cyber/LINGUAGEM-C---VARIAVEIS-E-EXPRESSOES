 /*  26. Leia um valor de área em metros quadrados m2 e apresente-o convertido em hectares.
 A fórmula de convers˜ão é: H = M * 0.0001, sendo M a área em metros quadrados e H
 a área em hectares.  */


#include <stdio.h>
#include <stdlib.h>

int main()
{
    float M, H;

    printf("Digite um valor em metros quadrados: \n");
    scanf("%f", &M);
    H = M * 0.0001;

    printf("O valor em hectares vale: %.2f \n", H);

    system("pause");
    return 0;
}
