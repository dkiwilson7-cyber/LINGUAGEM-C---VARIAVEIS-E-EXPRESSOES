/*  42. Receba o salário-base de um funcionário. Calcule e imprima o salário a receber, sabendo
se que esse funcionário tem uma gratificação de 5% sobre o salário-base. Além disso,
 ele paga 7% de imposto sobre o salário-base.  */


#include <stdio.h>
#include <stdlib.h>

int main()
{
    float salario_base, salario_final;

    printf("Digite o salario-base do funcionario: \n");
    scanf("%f", &salario_base);

    salario_final = salario_base + (salario_base*0.05) - (salario_base*0.07);

    printf("O salario a receber pelo funcionario vai ser: %.2f \n", salario_final);

    system("pause");
    return 0;
}
